const ROUTES = {
  login: '#/login/'.toUpperCase(),
  home: '#/home/'.toUpperCase(),
  articles: '#/articles/'.toUpperCase(),
  article: '#/article/'.toUpperCase(),
  author: '#/author/'.toUpperCase(),
  category: '#/category/'.toUpperCase(),
  test: '#/route3/'.toUpperCase(),
};

export default ROUTES;
