import formValidators from '@core/utils/formValidators';

const { required } = formValidators;

const validUsername = required;

const validPassword = required;

export { validUsername, validPassword };
