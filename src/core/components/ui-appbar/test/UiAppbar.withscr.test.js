import { screenshotTest } from '@core/testing';

screenshotTest({
  describe_: ['Test', 'Test1'],
  it_: ['visually looks correct', 'visually looks correct1'],
  id_: ['core-ui--uiappbar', 'core-ui--uiappbar'],
});
